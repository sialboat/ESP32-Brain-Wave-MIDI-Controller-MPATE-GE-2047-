// =======================================================================
// transition-table.h
// =======================================================================
// implementation of a transition table. Bring your own random or 
// pseudorandom generator. Otherwise use the weird one that we provide.

#include "cells.h"

// learn about interfaces in C++ so I can add just the variable logic

// base table class
class table
{
public:
private:
	int rows;
	int cols;
	
};
